.expo
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

node_modules

.expo
./node_modules

.env

.env.local
.env.development.local
.env.test.local
.env.production.local

.env.development
.env.test
.env.production


.DS_Store
.env.development.local
.env.test.local
.env.production.local
.env.local
.env.development
.env.test
.env.production
.env
.expo
.node_modules



  # ========================
# Node / Package managers
# ========================
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-lock.yaml
package-lock.json    # keep if you prefer npm lockfile; remove this line if you commit lockfiles

# ========================
# Environment files / secrets
# ========================
.env
.env.*                # .env.local, .env.development, .env.production, etc.
.env.local

# ========================
# React Native / Metro
# ========================
/.expo/
.expo-shared/
.expo/*
.expo-local/
metro-cache/          # if you created a custom cache folder
/metro-cache/
/tmp/metro-*
/tmp/haste-map-*
/bundles/
*.jsbundle

# ========================
# Android
# ========================
# Gradle
/android/.gradle/
/android/app/build/
/android/build/
/android/.idea/
/android/*.iml
**/build/
.gradle/
gradle-app.setting
gradle-wrapper.jar
.gradle/
local.properties      # contains local ANDROID_HOME path, usually shouldn't be committed
/.cxx
/capture/

# Keystore and signing files (never commit keys)
*.keystore
*.jks
keystores/*.keystore
android/keystores/
**/keystore-debug.keystore

# Google services / Firebase configs (often contain API keys)
android/app/google-services.json
**/google-services.json

# Android Studio files
.idea/
*.iml
*.hprof
/local.properties

# ========================
# iOS
# ========================
# Build products
/ios/build/
/ios/Build/
/ios/DerivedData/
/DerivedData/
*.xcarchive
*.xcuserstate
*.xcuserdata

# CocoaPods
ios/Pods/
/Pods/
/Podfile.lock        # keep if you want deterministic Pod versions; remove this line if you commit Podfile.lock

# Xcode workspace & schemes
# NOTE: If your team intentionally checks in workspace (e.g. generated workspace for pods),
# remove ios/*.xcworkspace from ignore list. By default Pods are ignored so workspace is safe to ignore.
ios/*.xcworkspace
*.xcodeproj/project.xcworkspace
!ios/Podfile         # keep Podfile in repo (it's the manifest)
*.mode1v3
*.mode2v3
*.pbxuser
*.perspectivev3

# iOS signing / secrets
*.p12
*.mobileprovision
**/GoogleService-Info.plist

# ========================
# IDE / Editor
# ========================
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# ========================
# OS files
# ========================
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# ========================
# Fastlane / CI / Build helpers
# ========================
.fastlane/report.xml
.fastlane/Preview.html
.fastlane/README.md
.fastlane/*key*       # if keys exist in fastlane folder
fastlane/report.xml
.build/

# ========================
# Tests / coverage
# ========================
/coverage/
jest-worker-*
/.nyc_output/

# ========================
# Misc / logs
# ========================
*.log
*.tmp
*.temp
*.orig

# ========================
# Optional / 3rd-party tools
# ========================
/react-native-debugger/
.sentrycli
.sentry-native

# ========================
# Editor swap files
# ========================
*~
*.swp
*.swo

# ========================
# Recommended: local config you might accidentally add
# ========================
.vscode/*
!.vscode/settings.json   # keep this if you intentionally share settings (remove the ! if you want to ignore all vscode)
!.vscode/tasks.json
!.vscode/launch.json

# ========================
# Notes
# If you use Expo, keep or remove the ios/ & android/ ignores depending on whether you eject.
# If you intend to commit CocoaPods Pods/Podfile.lock or android gradle wrappers for reproducible builds,
# remove the corresponding entries above. Adjust based on your team/CI workflow.
# ========================
